
# 1- Clase Biblioteca
# Crea una clase Biblioteca que represente una colección de libros. La clase debe tener los siguientes atributos privados:

# __nombre_biblioteca: El nombre de la biblioteca (cadena de texto).
# __direccion: La dirección de la biblioteca (cadena de texto).
# __libros: Una lista que contenga los títulos de los libros disponibles en la biblioteca (cadena de texto).
# Implementa los siguientes métodos:

# Métodos getter y setter para nombre_biblioteca.
# Métodos getter y setter para direccion.
# Un método getter para obtener la lista de libros get_libros().
# Un método agregar_libro(libro) que añada un libro a la lista de libros.
# Un método remover_libro(libro) que elimine un libro de la lista de libros si existe en la lista.
# Un método buscar_libro(libro) que devuelva True si el libro está en la lista y False en caso contrario.
# Un método listar_libros() que devuelva una cadena con todos los títulos de los libros disponibles, separados por comas.
# Un método reemplazar_libro(libro_viejo, libro_nuevo) que reemplace un libro viejo por uno nuevo si el libro viejo existe en la lista.
# Un método __str__ para representar el objeto como una cadena que muestre el nombre de la biblioteca y su dirección.


#=============================================================================================

#2- Clase EquipoDeFutbol
# Crea una clase EquipoDeFutbol que represente un equipo de fútbol. La clase debe tener los siguientes atributos privados:

# __nombre_equipo: El nombre del equipo (cadena de texto).
# __entrenador: El nombre del entrenador del equipo (cadena de texto).
# __jugadores: Una lista que contenga los nombres de los jugadores en el equipo (cadena de texto).
# Implementa los siguientes métodos:

# Métodos getter y setter para nombre_equipo.
# Métodos getter y setter para entrenador.
# Un método getter para obtener la lista de jugadores get_jugadores().
# Un método agregar_jugador(jugador) que añada un jugador a la lista de jugadores.
# Un método remover_jugador(jugador) que elimine un jugador de la lista de jugadores si existe en la lista.
# Un método listar_jugadores() que devuelva una cadena con todos los nombres de los jugadores en el equipo, separados por comas.
# Un método cambiar_capitan(capitan_nuevo) que reemplace el capitán actual por uno nuevo si el capitán nuevo está en la lista de jugadores.
# Un método __str__ para representar el objeto como una cadena que muestre el nombre del equipo y el nombre del entrenador.